[user]
	name = Stefan Zweifel
	email = hello@stefanzweifel.io
	signingkey = 49392B0C00D7AB90
[push]
	default = matching
[help]
    autocorrect = 1
[color]
    diff = auto
    status = auto
    branch = auto
    ui = true
[github]
    user = stefanzweifel
[core]
	editor = vim
	quotepath = false
	excludesfile = ~/.gitignore
    pager = delta
[mergetool]
	keepBackup = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[push]
    # See `git help config` (search for push.default)
    # for more information on different options of the below setting.
    #
    # Setting to git 2.0 default to suppress warning message
    default = simple

[alias]
    forget="! /opt/homebrew/bin/git fetch -p && /opt/homebrew/bin/git branch -vv | awk '/: gone]/{print $1}' | xargs /opt/homebrew/bin/git branch -D"
[init]
	defaultBranch = main

[includeIf "gitdir:~/Developer/git-auto-commit-action/"]
	path = "~/.dotfiles/git/gitconfig-git-auto-commit"

# Config for `git-delta`
[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections

[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
